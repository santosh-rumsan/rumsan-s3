info:
  name: Delete File
  type: http
  seq: 11

http:
  method: DELETE
  url: "{{url}}/api/buckets/test-bucket/objects/test.txt"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5