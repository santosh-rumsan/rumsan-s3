info:
  name: List Objects with Prefix
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/api/buckets/test-bucket/objects?prefix=docs/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5