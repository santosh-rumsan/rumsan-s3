info:
  name: Create Bucket
  type: http
  seq: 2

http:
  method: PUT
  url: "{{url}}/api/buckets/test-bucket"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5