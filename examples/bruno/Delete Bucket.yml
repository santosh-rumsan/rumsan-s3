info:
  name: Delete Bucket
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{url}}/api/buckets/test-bucket"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5