info:
  name: Download File
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/buckets/test-bucket/objects/test.txt"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
